async function renderPlaces(filter='All'){const filters=['All','Stop','Restaurant'];$('#filters').innerHTML=filters.map(x=>`<button class="filter ${x===filter?'active':''}" data-filter="${x}">${x}</button>`).join('');let out='';if(filter!=='Restaurant'){for(const d of days){for(const s of d.st){const src=await realImage(s.name);out+=`<article class="place">${src?`<img src="${src}" alt="${s.name}" loading="lazy">`:''}<div class="placeBody"><span class="tag">Day ${d.d} · Stop</span><h3>${s.name}</h3><p>${s.note}</p><a class="mapBtn secondary" target="_blank" rel="noopener" href="${googleSearch(s.name)}">Open in Google Maps</a></div></article>`}}}if(filter!=='Stop'){for(const d of days){for(const r of d.food){out+=`<article class="place"><div class="placeBody"><span class="tag">Night ${d.d} · Restaurant</span><h3>${r[0]}</h3><p>${r[1]}</p><a class="mapBtn secondary" target="_blank" rel="noopener" href="${googleSearch(r[0])}">Open in Google Maps</a></div></article>`}}}$('#placesList').innerHTML=out;$$('[data-filter]').forEach(b=>b.onclick=()=>renderPlaces(b.dataset.filter))}renderPlaces();